{
  "scripts": {
    "compile": "forge compile",
    "build": "forge build",
    "test": "forge test",
    "test:coverage": "forge coverage",
    "test:tba-mumbai": "forge fmt && forge test --fork-url $POLYGON_MUMBAI_RPC_URL --match-path test/foundry/TokenBoundAccount.t.sol",
    "test:tba-opsepolia": "forge fmt && forge test --fork-url $OPTIMISM_SEPOLIA_RPC_URL --match-path test/foundry/TokenBoundAccount.t.sol",
    "format": "forge fmt",
    "clean": "rm -rf cache out"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^1.1.1",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.1"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
